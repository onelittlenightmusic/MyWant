agents:
  - name: goal_thinker
    runtime: localGo
    type: think
    description: |
      Decomposes a user goal into sub-wants using a Python LLM script.
      Manages the full lifecycle: decompose → user approval → monitoring → replan.
    version: '1.0'
    capabilities:
      - goal_thinking
    role: thinker
