agents:
  - name: agent_premium
    runtime: localGo
    type: do
    capabilities:
      - hotel_agency
    uses:
      - xxx
    tracked_status_fields:
      - name: agent_result
        type: HotelSchedule
        description: Hotel reservation schedule with check-in/check-out times

  - name: hotel_monitor
    runtime: localGo
    type: monitor
    capabilities:
      - hotel_agency
    uses:
      - xxx
    tracked_status_fields:
      - name: agent_result
        type: HotelSchedule
        description: Hotel schedule loaded from YAML files