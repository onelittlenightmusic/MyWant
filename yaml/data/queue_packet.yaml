title: queue_packet
description: "A packet exchanged in queueing network simulations"
type: object
properties:
  num:
    type: integer
    description: "Packet sequence number"
  time:
    type: number
    description: "Timestamp of packet creation"
    default: 0.0
required:
  - num
