wants:
  - metadata:
      name: otp-pipeline
      type: "otp pipeline"
      labels:
        category: infrastructure
    spec:
      params:
        # policy_dir, use_llm, direction_map はレシピのデフォルト値を使用
        # data_dir はグローバルパラメータ otp_data_dir から自動取得
