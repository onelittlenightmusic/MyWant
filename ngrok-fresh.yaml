wants:
  - metadata:
      name: ngrok-fresh
      type: managed_launch
    spec:
      params:
        launcher: process
        command: ngrok
        args: '["http", "8080"]'
        url_regex: 'Forwarding\s+(https://[^\s]+)'
        result_field: tunnel_url
