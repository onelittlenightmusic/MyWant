wants:
  - metadata:
      name: "Flight Mock Server"
      type: live_server
      labels:
        service: mock
        component: flight-api
    spec:
      params:
        command: "./bin/flight-server"
        log_file: "logs/flight-server.log"
